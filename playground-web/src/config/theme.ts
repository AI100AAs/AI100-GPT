export const WINDOW_PADDING_HORIZONTAL = '20px'

/**
 * The reading column is deliberately narrow. Long lines of prose are hard to
 * scan, and every step here is mostly text plus one control.
 */
export const MAX_CONTENT_WIDTH = '780px'
